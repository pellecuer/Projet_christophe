{% extends 'base.html.twig' %}

{% block title %}Editer un pole{% endblock %}

{% block body %}
<div class="container">
    <div class="row col-md-6">
    <h1>Editer un pole</h1>
    {{ include('pole/_form.html.twig', {'button_label': 'Enregistrer'}) }}
    <button class"btn btn-default" href="{{ path('pole_index') }}">Retour à la liste</button>
    {{ include('pole/_delete_form.html.twig') }}    
    </div>
</div>
    
{% endblock %}
