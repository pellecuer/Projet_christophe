{% extends 'base.html.twig' %}

{% block title %}Pole{% endblock %}

{% block body %}
    <h1>Pole</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pole.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ pole.name }}</td>
            </tr>
            <tr>
                <th>Code_pole</th>
                <td>{{ pole.codePole }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pole_index') }}">back to list</a>

    <a href="{{ path('pole_edit', {'id': pole.id}) }}">edit</a>

    {{ include('pole/_delete_form.html.twig') }}
{% endblock %}
